<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.newlecture.web.cohort8th.repository.CategoryRepository"><!--어떤 인터페이스의 메서드인지-->
    <select id="findAll" resultType="Category"><!--반환타입은 qualifed name. yml에서 설정하면 안 써도 됨-->
        SELECT * FROM CATEGORY
    </select>
</mapper>